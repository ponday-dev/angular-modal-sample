import { ModalEntryDirective } from './modal-entry.directive';

describe('ModalEntryDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalEntryDirective();
    expect(directive).toBeTruthy();
  });
});
